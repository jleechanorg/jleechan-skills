---
description: Alias for /code-standards
type: quality
execution_mode: immediate
---

# /cs

This is a thin alias for `/code-standards`.

Load and follow `.claude/skills/code-standards/SKILL.md`, passing any command
arguments through as the review scope. Preserve `/code-standards` behavior:
dispatch adversarial, independent `/zfc`, `/zfclevel`, and
`/root-cause-first` review lanes.
