---
name: execute
description: Use when /execute is requested to plan, confirm authorized scope, and execute work.
---

# Execute

## Contract

Follow the complete compatibility contract in `references/legacy-command.md`. It
is the preserved command workflow for this exported portable skill; pass the
invoking command's arguments through unchanged.
