---
description: Evidence Standards — alias that reads the evidence-standards skill
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/evidence-standards/SKILL.md` completely, then execute it with `$ARGUMENTS`.
