---
description: Mirror the current dir/worktree/branch onto jeff-ubuntu (Linux) over SSH — or Slack hermes-pc if SSH is down — and continue the work session there.
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/linux-mirror/SKILL.md` completely, then execute it with `$ARGUMENTS`.
