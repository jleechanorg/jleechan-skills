---
description: Mirror the current dir/worktree/branch onto a Linux machine over SSH — Tailscale off-LAN, Slack as last resort — and continue the work session there.
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/linux-mirror/SKILL.md` completely, then execute it with `$ARGUMENTS`.
