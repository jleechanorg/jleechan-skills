---
description: Token-efficient second opinion — fans out Opus subagent + /research + /extended-library:secondo in parallel. Use instead of advisor() which ships the full conversation uncached.
aliases: [smart-advisor]
---

Read `~/.claude/skills/advice/SKILL.md` completely, then execute it with `$ARGUMENTS`.
