---
description: Steer work on a Mac via SSH (mirrors /linux but targets the Mac). Auto-detects local-vs-remote and chains to a Linux peer when needed.
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/mac-remote/SKILL.md` completely, then execute it with `$ARGUMENTS`.
