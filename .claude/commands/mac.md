---
description: Steer work on jleechan's MacBook via SSH (mirrors /linux but targets the MacBook). Chrome browser usage must be headless. Auto-detects local-vs-remote and chains to jeff-ubuntu when needed.
type: skill
execution_mode: immediate
legacy_reference: "references/mac\u002emd"
compatibility_reason: Legacy body preserved in a lazy compatibility reference.
---
Read `~/.claude/skills/legacy-command-compatibility/SKILL.md`; execute the exact `.md` path recorded by `legacy_reference` faithfully with `$ARGUMENTS`.
