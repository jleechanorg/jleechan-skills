---
description: Analyze failures and fix the harness (instructions, skills, tests, CI) rather than just the symptom
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/harness-engineering/SKILL.md` completely, then execute it with `$ARGUMENTS`.
