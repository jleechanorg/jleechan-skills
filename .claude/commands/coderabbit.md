---
description: "Post "@coderabbitai all good?" comment on the PR associated with the current branch"
type: skill
execution_mode: immediate
aliases:
- cr
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/coderabbit/SKILL.md` completely, then execute it with `$ARGUMENTS`.
