---
description: "/write-goal \u2014 mine last 30d of coding-CLI history + memory, then\
  \ write an outcome-driven goal spec wired for evidence, green-CI PRs, and high code\
  \ quality"
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/write-goal/SKILL.md` completely, then execute it with `$ARGUMENTS`.
