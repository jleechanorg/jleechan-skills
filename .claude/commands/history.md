---
description: /history — sparse conversation history search (Claude, Codex, Hermes) with quota guard
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/conversation-history-sparse/SKILL.md` completely, then execute it with `$ARGUMENTS`.
