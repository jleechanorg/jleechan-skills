---
description: Alias for /memory-search — search across configured memory systems.
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/memory-search/SKILL.md` completely, then execute it with `$ARGUMENTS`.
