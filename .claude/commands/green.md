---
description: /green - Verify and enforce the PR-Green Definition across all quality gates before PR creation or merge
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/pr-green-definition/SKILL.md` completely, then execute it with `$ARGUMENTS`.
