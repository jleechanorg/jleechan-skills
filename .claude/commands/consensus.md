---
description: /consensus - Multi-Agent Consensus Review
type: skill
execution_mode: immediate
aliases:
- cons
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/consensus/SKILL.md` completely, then execute it with `$ARGUMENTS`.
