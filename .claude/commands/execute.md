---
description: Execute Command - Plan-Approve-Execute Composition
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/execute/SKILL.md` completely, then execute it with `$ARGUMENTS`.
