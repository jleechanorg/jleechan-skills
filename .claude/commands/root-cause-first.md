---
description: /root-cause-first - diagnose and route an LLM/backend boundary failure
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/root-cause-first/SKILL.md` completely, then execute it in direct diagnostic mode with `$ARGUMENTS`.
