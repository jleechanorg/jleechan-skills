---
description: Ablation — measure a disputed root cause by replaying the real payload through the real path, one variable per arm, control first
aliases: [abal]
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/ablation/SKILL.md` completely, then execute it with `$ARGUMENTS`.
