---
description: /claw - Route tasks through Hermes gateway inference
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/claw-dispatch/SKILL.md` completely, then execute it with `$ARGUMENTS`.
