---
description: Run MCP smoke tests against preview server or local instance
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/smoke/SKILL.md` completely, then execute it with `$ARGUMENTS`.
