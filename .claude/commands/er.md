---
description: /er - Review an evidence bundle against Evidence Standards (/es) and verify strict conformance
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/evidence-review/SKILL.md` completely, then execute it with `$ARGUMENTS`.

When validating in-flight branch evidence against the target base branch, also follow `${CLAUDE_HOME:-$HOME/.claude}/skills/draft-first-pr/SKILL.md` for proper base remote and branch resolution.
