---
description: /repro — read repro-evidence skill (canonical); no logic here
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/repro-evidence/SKILL.md` completely, then execute it with `$ARGUMENTS`.
