---
description: Mirror the current dir/worktree/branch onto the MacBook over SSH — or Slack hermes (Mac) if SSH is down — and continue the work session there.
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/mac-mirror/SKILL.md` completely, then execute it with `$ARGUMENTS`.
