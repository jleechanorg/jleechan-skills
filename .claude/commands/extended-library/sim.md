---
description: Alias for /simulate - Predict next user prompt
type: ai
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/sim.md` completely, then execute it with `$ARGUMENTS`.
