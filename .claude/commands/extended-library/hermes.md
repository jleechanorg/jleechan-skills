---
description: Send a message to Hermes via the local gateway (localhost:8643) and show the response
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/hermes.md` completely, then execute it with `$ARGUMENTS`.
