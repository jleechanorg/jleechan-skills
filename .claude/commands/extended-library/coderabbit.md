---
description: Post "@coderabbitai all good?" comment on the PR associated with the current branch
type: git
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/coderabbit.md` completely, then execute it with `$ARGUMENTS`.
