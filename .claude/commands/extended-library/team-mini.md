---
description: MiniMax-M3 fan-out for parallel work on a specific prompt
execution_mode: immediate
type: skill
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/team-mini.md` completely, then execute it with `$ARGUMENTS`.
