---
description: Legacy compatibility dispatcher for /cmux-restore
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/cmux-restore.md` completely, then execute it with `$ARGUMENTS`.
