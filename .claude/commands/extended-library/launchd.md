---
description: Create and install a proper macOS launchd job using the canonical /launchd skill
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/launchd.md` completely, then execute it with `$ARGUMENTS`.
