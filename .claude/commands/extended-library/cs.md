---
description: Alias for /code-standards
type: quality
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/cs.md` completely, then execute it with `$ARGUMENTS`.
