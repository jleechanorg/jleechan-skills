---
description: Save evidence to /tmp structure
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/savetmp.md` completely, then execute it with `$ARGUMENTS`.
