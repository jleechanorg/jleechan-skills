---
description: Push Lite Command (pushl alias)
type: llm-orchestration
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/pushl.md` completely, then execute it with `$ARGUMENTS`.
