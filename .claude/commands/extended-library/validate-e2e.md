---
description: Legacy compatibility dispatcher for /validate-e2e
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/validate-e2e.md` completely, then execute it with `$ARGUMENTS`.
