---
description: Babysit — orchestration loop to monitor PRs, CI, and test evidence generation
type: orchestration
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/babysit.md` completely, then execute it with `$ARGUMENTS`.
