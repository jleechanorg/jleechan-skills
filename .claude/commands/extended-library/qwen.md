---
description: Legacy alias for Cerebras code generation (redirects to /cerebras)
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/qwen.md` completely, then execute it with `$ARGUMENTS`.
