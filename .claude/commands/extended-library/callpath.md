---
description: /callpath — trace execution across hops, services, and call stacks (read-only)
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/callpath.md` completely, then execute it with `$ARGUMENTS`.
