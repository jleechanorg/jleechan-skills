---
description: Use Agent Orchestrator with strict parameter fidelity, post-spawn verification, and pre-spawn cap cleanup
type: workflow
execution_mode: deferred
scope: user
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/ao.md` completely, then execute it with `$ARGUMENTS`.
