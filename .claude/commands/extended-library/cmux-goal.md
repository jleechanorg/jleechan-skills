---
description: /cmux-goal — set the builtin /goal for yourself via the cmux socket CLI (Stop hook + purple indicator)
type: llm-orchestration
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/cmux-goal.md` completely, then execute it with `$ARGUMENTS`.
