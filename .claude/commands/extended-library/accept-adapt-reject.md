---
description: Triage incoming feedback into Accept / Adapt / Reject buckets with structured verdicts and follow-up actions.
type: ai
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/accept-adapt-reject.md` completely, then execute it with `$ARGUMENTS`.
