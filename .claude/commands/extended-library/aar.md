---
description: Alias for /accept-adapt-reject. Triage feedback into Accept / Adapt / Reject buckets.
type: ai
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/aar.md` completely, then execute it with `$ARGUMENTS`.
