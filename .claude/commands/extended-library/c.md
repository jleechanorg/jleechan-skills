---
description: Generate large amounts of code using Cerebras (alias for /cerebras)
type: skill
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/c.md` completely, then execute it with `$ARGUMENTS`.
