---
description: Investigate dice integrity warnings for a campaign (your-project.com only — queries GCP project worldarchitecture-ai and the dice service).
type: llm-orchestration
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/investigatedice.md` completely, then execute it with `$ARGUMENTS`.
