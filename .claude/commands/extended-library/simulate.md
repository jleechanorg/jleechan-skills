---
description: Predict next user prompt using $USER simulation
type: ai
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/simulate.md` completely, then execute it with `$ARGUMENTS`.
