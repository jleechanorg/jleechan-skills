---
description: /smoke-local - REAL local server + agy LLM smoke: new campaign → level 4 + combat/spell/inventory/dialog
type: testing
execution_mode: immediate
---

Read `${CLAUDE_HOME:-$HOME/.claude}/skills/extended-library/SKILL.md` and `references/extended-library/smoke-local.md` completely, then execute it with `$ARGUMENTS`.
