# Archived loose skill `.md` files

These were top-level `*.md` entries under `~/.claude/skills/` (not `SKILL.md` directories). Consolidated under `_archive/loose-md/` (bd-2w17).

## Current canonical location (2026-04-05)

Restored copies live as **`~/.claude/skills/<name>/SKILL.md`** (see **`agent-orchestrator`** `roadmap/skill-restoration.md`). Treat this folder as **history / backup**, not the source of truth.

## Codex

**`~/.codex/skills/*.md`** symlinks that pointed at removed loose paths were updated to target **`SKILL.md`** under the same skill name (e.g. `harness-engineering.md` → `harness-engineering/SKILL.md`).

## Retention

- **Backup snapshot:** `~/.claude/backups/skills-archived-loose-md-2026-04-05.tar.gz` (and see `~/.claude/backups/README.md`).
- You may **delete** this directory tree after confirming you do not need line-by-line diff against the restored `SKILL.md` copies — **disk space only**; behavior does not depend on these files.
