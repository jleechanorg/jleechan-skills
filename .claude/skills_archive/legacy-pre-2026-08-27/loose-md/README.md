# Archived loose skill `.md` files

These were top-level `*.md` entries under `~/.claude/skills/` (not `SKILL.md` directories). Moved here to reduce skill-list noise (bd-2w17). Restore by moving a file back to `../` if you still use it as a single-file skill.

**Backup snapshot:** `~/.claude/backups/skills-archived-loose-md-2026-04-05.tar.gz` (and see `~/.claude/backups/README.md`).
